# Changesets

This directory holds [changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to describe a change; the release workflow turns them into
version bumps and a changelog entry.
